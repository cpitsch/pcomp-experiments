Because of access restrictions on the MIMIC dataset, we do not provide the results directly.
Instead, we provide the SQL queries and post-processing scripts necessary to create the event
log used in the analysis. The queries are found in `queries`, and when the results are placed
in the `results` directory, the post-processing script `process_results_to_xes.py` creates the event log.
